name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        default: dev
  push:
    branches: [main]
    tags: ["v*"]

permissions:
  id-token: write # required for Azure OIDC login
  contents: read

env:
  RESOURCE_GROUP: rg-appt-dev
  AKS_CLUSTER: aks-appt-dev

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    strategy:
      matrix:
        include:
          - name: web-form
            dockerfile: services/web-form/Dockerfile
          - name: agent-orchestrator
            dockerfile: services/agent-orchestrator/Dockerfile
          - name: mcp-resource-details
            dockerfile: mcp-servers/mcp-resource-details/Dockerfile
          - name: mcp-calendar
            dockerfile: mcp-servers/mcp-calendar/Dockerfile
          - name: mcp-email
            dockerfile: mcp-servers/mcp-email/Dockerfile
          - name: db-migrate
            dockerfile: db/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Compute tag
        id: meta
        run: echo "tag=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: ACR login
        run: az acr login --name ${{ secrets.ACR_NAME }}

      - name: Build & push (${{ matrix.name }})
        run: |
          IMAGE=${{ secrets.ACR_LOGIN_SERVER }}/${{ matrix.name }}:${{ steps.meta.outputs.tag }}
          docker build -f ${{ matrix.dockerfile }} -t "$IMAGE" .
          docker push "$IMAGE"

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - uses: actions/checkout@v4

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Get AKS credentials
        run: az aks get-credentials --resource-group $RESOURCE_GROUP --name $AKS_CLUSTER

      - name: Helm upgrade
        run: |
          helm upgrade --install appointment-scheduler deploy/helm \
            --namespace appointments --create-namespace \
            --set image.registry=${{ secrets.ACR_LOGIN_SERVER }} \
            --set image.tag=${{ needs.build-and-push.outputs.tag }} \
            --set fakeProviders=false \
            --wait